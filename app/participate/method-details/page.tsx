// STUB — out of scope for CA-014A. Exists only so the Continue action on
// Choose Causes has a real route to navigate to. Not a designed screen.
export default function MethodDetailsStubPage() {
  return (
    <main className="mx-auto max-w-[800px] px-5 py-10 font-sans text-(--color-text-secondary)">
      <p>Method Details — not yet built (out of scope for CA-014A).</p>
    </main>
  );
}
